<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Anna Kras | Portfolio</title>
    <style>
        :root {
            --bg-color: #0a1128; /* Глубокий синий - Козерог/Море */
            --accent-color: #d4af37; /* Золото - Лев */
            --text-color: #f4f4f4;
            --font-main: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            background-color: var(--bg-color);
            color: var(--text-color);
            font-family: var(--font-main);
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* Кнопка смены языка */
       .lang-switch {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 100;
            background: none;
            border: 1px solid var(--accent-color);
            color: var(--accent-color);
            padding: 5px 15px;
            cursor: pointer;
            font-weight: bold;
            transition: 0.3s;
        }
       .lang-switch:hover { background: var(--accent-color); color: var(--bg-color); }

        /* Шапка */
        header {
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            text-align: center;
        }

       .hero-image {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            object-fit: cover;
            opacity: 0.4;
            filter: grayscale(30%);
        }

       .hero-content h1 {
            font-size: 10vw;
            line-height: 0.8;
            text-transform: uppercase;
            letter-spacing: -5px;
            position: relative;
            z-index: 2;
        }

        /* Секция Statement */
       .section { padding: 100px 5%; max-width: 1400px; margin: 0 auto; }

       .statement-text {
            font-size: 2.5rem;
            font-weight: 300;
            line-height: 1.2;
            margin-bottom: 50px;
        }

        /* Нестандартная сетка галереи */
       .gallery {
            display: grid;
            grid-template-columns: repeat(12, 1fr);
            gap: 20px;
        }

       .gallery-item {
            background: #1a2544;
            position: relative;
            overflow: hidden;
            transition: 0.5s;
        }

       .gallery-item img {
            width: 100%; height: 100%; object-fit: cover;
            transition: transform 0.8s ease;
        }

       .gallery-item:hover img { transform: scale(1.05); }

        /* Ломаная сетка */
       .item-lg { grid-column: 1 / 8; height: 500px; }
       .item-sm { grid-column: 8 / 13; height: 350px; align-self: end; }
       .item-md { grid-column: 2 / 6; height: 400px; margin-top: -50px; }
       .item-circ { 
            grid-column: 7 / 11; 
            height: 400px; 
            border-radius: 50%; 
            margin-top: 50px; 
        }

        /* CV Секция */
       .cv-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 50px;
            border-top: 1px solid #333;
            padding-top: 50px;
        }

       .cv-column h3 {
            color: var(--accent-color);
            font-size: 2rem;
            margin-bottom: 20px;
            text-transform: uppercase;
        }

       .cv-item { margin-bottom: 15px; }
       .cv-year { font-weight: bold; margin-right: 10px; }

        /* Анимация при скролле */
       .reveal {
            opacity: 0;
            transform: translateY(50px);
            transition: all 1s ease-out;
        }
       .reveal.active {
            opacity: 1;
            transform: translateY(0);
        }

        footer {
            padding: 50px;
            text-align: center;
            border-top: 1px solid #333;
        }

        @media (max-width: 768px) {
           .cv-grid { grid-template-columns: 1fr; }
           .gallery { display: block; }
           .gallery-item { margin-bottom: 20px; height: 300px!important; border-radius: 0!important; }
           .statement-text { font-size: 1.5rem; }
        }
    </style>
</head>
<body>

    <button class="lang-switch" onclick="toggleLang()">EN / RU</button>

    <header>
        <img src="https://lh3.googleusercontent.com/d/1A-v0Fv6z6z6z6z6z6z6z6z6" class="hero-image" alt="Anna Kras">
        <div class="hero-content">
            <h1>ANNA KRAS</h1>
            <p style="letter-spacing: 5px; color: var(--accent-color);">ARTIST | ХУДОЖНИК</p>
        </div>
    </header>

    <section class="section reveal" id="statement">
        <div class="statement-text lang-ru">
            "В своих картинах я обращаюсь к внутреннему ребенку каждого — к триггерам, точкам формирования нейронных связей... Вода соединяет прошлое и настоящее."
        </div>
        <div class="statement-text lang-en" style="display:none;">
            "In my paintings, I appeal to the inner child within everyone — to the triggers, the points where neural connections are formed... Water connects the past and the present."
        </div>
    </section>

    <section class="section reveal">
        <h2 style="font-size: 3rem; margin-bottom: 30px;">GALLERY</h2>
        <div class="gallery">
            <div class="gallery-item item-lg"><img src="https://lh3.googleusercontent.com/d/1B-v1Fv6z6z6z6z6z6z6z6z6" alt="Sea"></div>
            <div class="gallery-item item-sm"><img src="https://lh3.googleusercontent.com/d/1C-v2Fv6z6z6z6z6z6z6z6z6" alt="Swimmer"></div>
            <div class="gallery-item item-md"><img src="https://lh3.googleusercontent.com/d/1D-v3Fv6z6z6z6z6z6z6z6z6" alt="Art"></div>
            <div class="gallery-item item-circ"><img src="https://lh3.googleusercontent.com/d/1E-v4Fv6z6z6z6z6z6z6z6z6" alt="Быстрее"></div>
        </div>
    </section>

    <section class="section reveal" id="cv">
        <div class="cv-grid">
            <div class="cv-column">
                <h3 class="lang-ru">Выставки</h3>
                <h3 class="lang-en" style="display:none;">Exhibitions</h3>
                <div class="cv-item"><span class="cv-year">2